import { Options, ReturnObject, Options2 } from './interfaces';
declare function convertor(time: string): number;
declare function convertor(time: string, options: Options): number;
declare function convertor(time: number): string;
declare function convertor(time: number, options: Options2): ReturnObject;
declare function convertor(time: number, options: Options): string;
export default convertor;
